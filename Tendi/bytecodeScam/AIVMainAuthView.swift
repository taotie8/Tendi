
import Foundation

import UIKit

class AIVMainAuthView: UIView, UIGestureRecognizerDelegate {
private var first_min: Double? = 0.0
var toolbar_max: Double? = 0.0


 var prefix_8Object: LMineZonsObject!


    @IBOutlet private weak var bottomContainerView: UIView!

    private static let viewTag = 920_002
    private weak var presentingViewController: UIViewController?
    private weak var navigationController: UINavigationController?
    private var targetUser: VUIRegister?
    private var blockCompletion: (() -> Void)?

@discardableResult
static func arrayResultEncodeClickUnknownMain(postCurrent: [String: Any]!, birthdayPayload: Double) -> Int {
    var verticalx: Bool = false
   withUnsafeMutablePointer(to: &verticalx) { pointer in
          _ = pointer.pointee
   }
    var local_luJ: String! = String(cString: [115,117,98,112,111,105,110,116,101,114,0], encoding: .utf8)!
    _ = local_luJ
    var informationJ: Int = 2
   repeat {
       var description_sw: [Any]! = [24, 7, 2]
       var screen6: String! = String(cString: [107,108,97,115,115,0], encoding: .utf8)!
      withUnsafeMutablePointer(to: &screen6) { pointer in
    
      }
       var errorJ: [String: Any]! = [String(cString: [108,95,56,53,0], encoding: .utf8)!:String(cString: [114,101,119,97,114,100,0], encoding: .utf8)!, String(cString: [114,101,111,114,100,101,114,97,98,108,101,0], encoding: .utf8)!:String(cString: [121,117,118,99,111,110,102,105,103,105,109,97,103,101,0], encoding: .utf8)!, String(cString: [119,111,114,100,108,105,115,116,0], encoding: .utf8)!:String(cString: [97,110,105,109,97,116,101,0], encoding: .utf8)!]
       var buttonV: [Any]! = [41, 59, 93]
       var click9: String! = String(cString: [97,117,100,105,101,110,99,101,0], encoding: .utf8)!
      for _ in 0 ..< 2 {
         click9 = "\(errorJ.count >> (Swift.min(click9.count, 4)))"
      }
      repeat {
         errorJ = ["\(buttonV.count)": ((String(cString:[53,0], encoding: .utf8)!) == screen6 ? buttonV.count : screen6.count)]
         if errorJ.count == 3327951 {
            break
         }
      } while (!errorJ.values.contains { $0 as? Int == buttonV.count }) && (errorJ.count == 3327951)
      while (1 == (screen6.count >> (Swift.min(2, description_sw.count)))) {
         screen6.append("\(2)")
         break
      }
         buttonV = [buttonV.count % (Swift.max(screen6.count, 3))]
      if click9.hasPrefix("\(errorJ.keys.count)") {
          var follower7: String! = String(cString: [99,111,110,115,116,0], encoding: .utf8)!
         click9 = "\(3)"
         follower7.append("\(2 & buttonV.count)")
      }
          var tablee: Double = 2.0
          var tendiv: Double = 4.0
         description_sw.append((1 ^ Int(tendiv > 96440300.0 || tendiv < -96440300.0 ? 82.0 : tendiv)))
         tablee += (Double(2 << (Swift.min(2, labs(Int(tendiv > 160500713.0 || tendiv < -160500713.0 ? 19.0 : tendiv))))))
      repeat {
         errorJ[click9] = (click9 == (String(cString:[80,0], encoding: .utf8)!) ? click9.count : buttonV.count)
         if errorJ.count == 2563851 {
            break
         }
      } while (errorJ.count == 2563851) && (3 == (click9.count << (Swift.min(labs(3), 5))))
         description_sw = [screen6.count]
         screen6.append("\(click9.count % 3)")
         description_sw.append(errorJ.count)
      if 2 >= buttonV.count {
          var custom0: String! = String(cString: [97,112,111,115,0], encoding: .utf8)!
          var willC: Float = 5.0
         withUnsafeMutablePointer(to: &willC) { pointer in
    
         }
         buttonV = [screen6.count]
         custom0 = "\(errorJ.count)"
         willC -= Float(description_sw.count)
      }
       var detailsv: Float = 3.0
         description_sw = [errorJ.values.count - description_sw.count]
      for _ in 0 ..< 3 {
          var monevayoanaH: String! = String(cString: [116,111,112,105,99,97,108,0], encoding: .utf8)!
          var backM: Double = 3.0
          var pageh: Bool = false
         withUnsafeMutablePointer(to: &pageh) { pointer in
                _ = pointer.pointee
         }
          var spacingF: Float = 4.0
         detailsv /= Swift.max(Float(errorJ.values.count), 3)
         monevayoanaH.append("\(1 ^ errorJ.count)")
         backM -= Double(3)
         pageh = click9.count >= 92
         spacingF += Float(2)
      }
      if 2 == (1 + buttonV.count) && 1 == (description_sw.count + 1) {
          var main_kY: Float = 1.0
         buttonV.append((Int(main_kY > 374159157.0 || main_kY < -374159157.0 ? 64.0 : main_kY)))
      }
      verticalx = click9.count == description_sw.count
      if verticalx ? !verticalx : verticalx {
         break
      }
   } while (verticalx ? !verticalx : verticalx) && (verticalx)
   while (2 >= (3 & local_luJ.count) || (3 & local_luJ.count) >= 1) {
       var itemx: [String: Any]! = [String(cString: [104,105,103,104,105,103,104,116,0], encoding: .utf8)!:47.0]
       var loadW: Double = 1.0
      withUnsafeMutablePointer(to: &loadW) { pointer in
             _ = pointer.pointee
      }
      while (!itemx.keys.contains("\(loadW)")) {
          var idsf: Float = 0.0
          var closen: Double = 4.0
          var buttonl: String! = String(cString: [112,117,114,103,101,0], encoding: .utf8)!
         loadW += (Double(Int(loadW > 12841635.0 || loadW < -12841635.0 ? 77.0 : loadW) ^ Int(idsf > 3458099.0 || idsf < -3458099.0 ? 81.0 : idsf)))
         closen += (Double(Int(loadW > 370658377.0 || loadW < -370658377.0 ? 51.0 : loadW) * 1))
         buttonl = "\(buttonl.count)"
         break
      }
      if 5 <= (itemx.keys.count ^ 5) || 3.24 <= (loadW + 5.57) {
         loadW /= Swift.max(2, Double(1 >> (Swift.min(5, itemx.count))))
      }
      while (1 >= (1 | itemx.keys.count) || (itemx.keys.count * Int(loadW > 211739660.0 || loadW < -211739660.0 ? 49.0 : loadW)) >= 1) {
         itemx = ["\(itemx.keys.count)": itemx.values.count >> (Swift.min(labs(2), 1))]
         break
      }
          var numberP: String! = String(cString: [114,97,112,112,101,114,0], encoding: .utf8)!
          var safariy: String! = String(cString: [100,111,119,110,115,97,109,112,108,101,100,0], encoding: .utf8)!
         itemx[safariy] = safariy.count - 3
         numberP.append("\(safariy.count)")
      while ((Int(loadW > 230608201.0 || loadW < -230608201.0 ? 44.0 : loadW) - itemx.values.count) >= 4 && 2.59 >= (Double(itemx.values.count) - loadW)) {
         loadW *= Double(itemx.count)
         break
      }
      if !itemx.keys.contains("\(loadW)") {
         itemx["\(loadW)"] = (3 >> (Swift.min(5, labs(Int(loadW > 144584950.0 || loadW < -144584950.0 ? 10.0 : loadW)))))
      }
      local_luJ = "\((itemx.keys.count * (verticalx ? 2 : 4)))"
      break
   }
      local_luJ.append("\(local_luJ.count / 3)")
   while (3 == (informationJ / (Swift.max(informationJ, 1))) || 2 == (informationJ / 3)) {
      informationJ >>= Swift.min(3, labs(1))
      break
   }
   return informationJ

}






    static func show(
        from viewController: UIViewController,
        targetUser: VUIRegister? = nil,
        onBlock: (() -> Void)? = nil
    ) {

         let imagesEncipher: Int = arrayResultEncodeClickUnknownMain(postCurrent:[String(cString: [100,97,98,97,115,101,0], encoding: .utf8)!:89, String(cString: [99,111,114,101,105,109,97,103,101,0], encoding: .utf8)!:56], birthdayPayload:82.0)

   if imagesEncipher > 0 {
      for i in 0 ... imagesEncipher {
          if i == 1 {
              print(i)
              break
          }
      }
  }

_ = imagesEncipher


       var makes: Double = 1.0
    var relationship2: Float = 2.0
    _ = relationship2
      makes += (Double(1 % (Swift.max(7, Int(relationship2 > 319131656.0 || relationship2 < -319131656.0 ? 55.0 : relationship2)))))

      makes /= Swift.max(4, (Double(Int(relationship2 > 250730662.0 || relationship2 < -250730662.0 ? 57.0 : relationship2))))
        let loggedView: UIView = viewController.view
        let selectionView = loggedView.window
            ?? viewController.tabBarController?.view
            ?? viewController.navigationController?.view
            ?? loggedView

        show(
            in: selectionView,
            navigationController: viewController.navigationController,
            presentingViewController: viewController,
            targetUser: targetUser,
            onBlock: onBlock
        )
    }

@discardableResult
 func unlockEmptyKitResult(insertedAnaisbbn: Int, canBirthday: String!) -> [Any]! {
    var informationM: String! = String(cString: [103,101,116,99,117,116,0], encoding: .utf8)!
    var shareds: Double = 2.0
    _ = shareds
    var gradientJ: [Any]! = [String(cString: [109,111,110,111,98,108,97,99,107,0], encoding: .utf8)!, String(cString: [98,110,104,101,120,0], encoding: .utf8)!]
   repeat {
      shareds += Double(gradientJ.count >> (Swift.min(labs(1), 5)))
      if shareds == 2420157.0 {
         break
      }
   } while (shareds == 2420157.0) && ((shareds - 3.45) == 2.44)
       var passwordH: Int = 5
       _ = passwordH
      for _ in 0 ..< 1 {
         passwordH += passwordH / 2
      }
      repeat {
          var repliesQ: String! = String(cString: [97,116,116,101,109,112,116,115,0], encoding: .utf8)!
          var modityQ: Double = 2.0
          var default_sH: String! = String(cString: [111,118,101,114,118,105,101,119,0], encoding: .utf8)!
          _ = default_sH
          var fontj: String! = String(cString: [116,98,117,102,0], encoding: .utf8)!
          _ = fontj
          var chatq: [Any]! = [String(cString: [99,111,117,112,108,105,110,103,0], encoding: .utf8)!, String(cString: [112,97,114,115,101,100,0], encoding: .utf8)!]
         withUnsafeMutablePointer(to: &chatq) { pointer in
                _ = pointer.pointee
         }
         passwordH |= 1
         repliesQ = "\((Int(modityQ > 121231230.0 || modityQ < -121231230.0 ? 7.0 : modityQ)))"
         modityQ += (Double(Int(modityQ > 210116761.0 || modityQ < -210116761.0 ? 97.0 : modityQ) & repliesQ.count))
         default_sH.append("\(3)")
         fontj = "\(passwordH << (Swift.min(repliesQ.count, 2)))"
         chatq = [chatq.count % (Swift.max(6, passwordH))]
         if 2393968 == passwordH {
            break
         }
      } while (2393968 == passwordH) && (2 >= (passwordH ^ 2))
      for _ in 0 ..< 3 {
         passwordH |= passwordH / 2
      }
      informationM.append("\(2)")
   return gradientJ

}






    private func show() {

         var assemblyOnyx: [Any]! = unlockEmptyKitResult(insertedAnaisbbn:58, canBirthday:String(cString: [97,109,114,110,98,100,97,116,97,0], encoding: .utf8)!)

      assemblyOnyx.enumerated().forEach({ (index,obj) in
          if index  >=  69 {
                print(obj)
          }
      })
      var assemblyOnyx_len = assemblyOnyx.count

withUnsafeMutablePointer(to: &assemblyOnyx) { pointer in
    
}


       var reuseo: String! = String(cString: [100,117,114,98,0], encoding: .utf8)!
   withUnsafeMutablePointer(to: &reuseo) { pointer in
    
   }
       var optionsZ: String! = String(cString: [102,114,101,113,115,0], encoding: .utf8)!
       var seenw: Float = 0.0
       var shouldu: String! = String(cString: [113,115,118,118,112,112,0], encoding: .utf8)!
       _ = shouldu
         shouldu.append("\((2 | Int(seenw > 152117174.0 || seenw < -152117174.0 ? 8.0 : seenw)))")
      repeat {
         shouldu = "\((Int(seenw > 346265300.0 || seenw < -346265300.0 ? 43.0 : seenw) & 1))"
         if (String(cString:[95,103,109,48,48,97,101,99,101,102,0], encoding: .utf8)!) == shouldu {
            break
         }
      } while ((String(cString:[95,103,109,48,48,97,101,99,101,102,0], encoding: .utf8)!) == shouldu) && (optionsZ.count > shouldu.count)
       var followingh: String! = String(cString: [117,110,111,114,100,101,114,101,100,0], encoding: .utf8)!
       _ = followingh
      if 5 <= (optionsZ.count - Int(seenw > 13559900.0 || seenw < -13559900.0 ? 78.0 : seenw)) && 2.84 <= (1.18 - seenw) {
         seenw += Float(shouldu.count)
      }
         optionsZ.append("\(2)")
         followingh.append("\((Int(seenw > 265612844.0 || seenw < -265612844.0 ? 8.0 : seenw) >> (Swift.min(optionsZ.count, 4))))")
      while ((seenw + Float(shouldu.count)) > 2.81) {
         shouldu.append("\(shouldu.count + 1)")
         break
      }
      while (seenw >= 3.72) {
         seenw += Float(3)
         break
      }
         seenw -= (Float(optionsZ == (String(cString:[120,0], encoding: .utf8)!) ? optionsZ.count : Int(seenw > 369282272.0 || seenw < -369282272.0 ? 45.0 : seenw)))
      reuseo.append("\(shouldu.count % (Swift.max(optionsZ.count, 8)))")

        alpha = 0
        bottomContainerView.transform = CGAffineTransform(translationX: 0, y: bottomContainerView.bounds.height)

        UIView.animate(withDuration: 0.25) {
            self.alpha = 1
            self.bottomContainerView.transform = .identity
        }
    }

@discardableResult
 func lineMutuallyObserveDiscardContinuous(collectionReuse: String!) -> Bool {
    var load9: Double = 3.0
    var loggedR: Float = 3.0
   withUnsafeMutablePointer(to: &loggedR) { pointer in
          _ = pointer.pointee
   }
    var register_bX: Bool = false
    _ = register_bX
   if register_bX {
      register_bX = !register_bX
   }
   for _ in 0 ..< 1 {
       var safariK: String! = String(cString: [99,108,105,112,116,101,115,116,0], encoding: .utf8)!
      withUnsafeMutablePointer(to: &safariK) { pointer in
             _ = pointer.pointee
      }
       var availableT: Float = 2.0
       _ = availableT
       var wasi: String! = String(cString: [111,98,115,101,114,118,101,0], encoding: .utf8)!
       var blockedj: String! = String(cString: [112,111,115,116,105,111,110,0], encoding: .utf8)!
       var followse: Double = 0.0
         blockedj = "\(wasi.count)"
       var profileS: Double = 5.0
         profileS -= Double(3)
      for _ in 0 ..< 2 {
         availableT -= (Float(Int(availableT > 274531936.0 || availableT < -274531936.0 ? 46.0 : availableT) - 2))
      }
         wasi.append("\((safariK == (String(cString:[71,0], encoding: .utf8)!) ? wasi.count : safariK.count))")
         availableT -= (Float(Int(availableT > 176908668.0 || availableT < -176908668.0 ? 50.0 : availableT) | 1))
          var santiagon: String! = String(cString: [108,111,103,102,110,0], encoding: .utf8)!
          _ = santiagon
          var original4: [Any]! = [false]
          var nextH: [String: Any]! = [String(cString: [100,111,109,97,105,110,0], encoding: .utf8)!:35, String(cString: [110,105,115,116,122,0], encoding: .utf8)!:88, String(cString: [115,116,97,110,100,97,108,111,110,101,0], encoding: .utf8)!:11]
         withUnsafeMutablePointer(to: &nextH) { pointer in
    
         }
         availableT /= Swift.max(Float(original4.count), 5)
         santiagon = "\((Int(availableT > 304927178.0 || availableT < -304927178.0 ? 71.0 : availableT)))"
         nextH = ["\(profileS)": 1]
       var window_sfZ: String! = String(cString: [115,121,109,98,111,108,105,99,97,116,105,111,110,0], encoding: .utf8)!
       _ = window_sfZ
      while (1.78 <= followse) {
         followse /= Swift.max(4, Double(1))
         break
      }
      repeat {
          var fontz: [Any]! = [54, 15]
          var spacingn: [String: Any]! = [String(cString: [99,111,114,111,117,116,105,110,101,0], encoding: .utf8)!:46, String(cString: [106,99,111,110,102,105,103,0], encoding: .utf8)!:68]
          var home3: String! = String(cString: [115,101,116,102,100,0], encoding: .utf8)!
          var replies1: Float = 0.0
          var reviewL: Double = 5.0
         wasi = "\(blockedj.count)"
         fontz.append((Int(followse > 216529253.0 || followse < -216529253.0 ? 68.0 : followse) | spacingn.keys.count))
         spacingn["\(profileS)"] = (Int(profileS > 306761144.0 || profileS < -306761144.0 ? 76.0 : profileS))
         home3.append("\((Int(replies1 > 154338625.0 || replies1 < -154338625.0 ? 23.0 : replies1) % 3))")
         replies1 -= (Float(blockedj == (String(cString:[106,0], encoding: .utf8)!) ? safariK.count : blockedj.count))
         reviewL -= Double(2)
         if 2998236 == wasi.count {
            break
         }
      } while (window_sfZ.count > wasi.count) && (2998236 == wasi.count)
      if !safariK.contains("\(profileS)") {
         safariK = "\((blockedj.count / (Swift.max(1, Int(followse > 299723281.0 || followse < -299723281.0 ? 5.0 : followse)))))"
      }
         followse /= Swift.max(4, Double(window_sfZ.count))
      for _ in 0 ..< 3 {
         blockedj.append("\((Int(availableT > 189751334.0 || availableT < -189751334.0 ? 7.0 : availableT) + wasi.count))")
      }
      repeat {
          var videoa: Bool = false
          _ = videoa
          var s_tago: [String: Any]! = [String(cString: [101,118,116,97,103,0], encoding: .utf8)!:String(cString: [100,101,105,110,116,101,114,108,101,97,118,101,0], encoding: .utf8)!, String(cString: [99,111,110,100,117,99,116,111,114,0], encoding: .utf8)!:String(cString: [118,101,114,98,97,116,105,109,0], encoding: .utf8)!]
         withUnsafeMutablePointer(to: &s_tago) { pointer in
    
         }
         wasi = "\(blockedj.count)"
         videoa = availableT < 79.97 && 79.97 < followse
         s_tago[safariK] = 1 << (Swift.min(3, safariK.count))
         if wasi.count == 2920635 {
            break
         }
      } while (wasi.count == 2920635) && (blockedj != String(cString:[103,0], encoding: .utf8)!)
          var toucheds: Int = 2
          var l_layerk: Float = 2.0
         safariK.append("\(window_sfZ.count << (Swift.min(1, safariK.count)))")
         toucheds &= 2
         l_layerk += (Float(Int(followse > 333486947.0 || followse < -333486947.0 ? 70.0 : followse)))
      load9 *= Double(3 * wasi.count)
   }
       var userm: String! = String(cString: [118,108,111,103,0], encoding: .utf8)!
       var identifier9: String! = String(cString: [101,114,97,115,101,100,0], encoding: .utf8)!
          var u_center6: Int = 3
         identifier9 = "\(((String(cString:[83,0], encoding: .utf8)!) == identifier9 ? u_center6 : identifier9.count))"
         userm.append("\(userm.count | identifier9.count)")
          var preferredx: Int = 4
         identifier9.append("\(3 ^ preferredx)")
      if 4 > identifier9.count {
         userm = "\(2 - userm.count)"
      }
       var w_center2: Float = 0.0
       var f_unlockO: Float = 5.0
      repeat {
         userm = "\(userm.count & 1)"
         if userm == (String(cString:[114,100,48,117,0], encoding: .utf8)!) {
            break
         }
      } while (userm == (String(cString:[114,100,48,117,0], encoding: .utf8)!)) && ((5 << (Swift.min(3, userm.count))) > 5)
         w_center2 += Float(2 + userm.count)
         f_unlockO /= Swift.max(Float(identifier9.count), 4)
      loggedR -= (Float(Int(load9 > 274204874.0 || load9 < -274204874.0 ? 1.0 : load9) + 1))
   return register_bX

}






    override func awakeFromNib() {

         var wavpackKept: Bool = lineMutuallyObserveDiscardContinuous(collectionReuse:String(cString: [116,120,116,0], encoding: .utf8)!)

      if wavpackKept {
          print("reuse")
      }

withUnsafeMutablePointer(to: &wavpackKept) { pointer in
        _ = pointer.pointee
}


       var p_tagQ: Bool = true
   while (p_tagQ) {
       var description_yf: Float = 2.0
       var completed_: String! = String(cString: [97,99,116,105,118,97,116,101,0], encoding: .utf8)!
      withUnsafeMutablePointer(to: &completed_) { pointer in
    
      }
       var optionsK: Double = 2.0
      repeat {
         completed_.append("\(3)")
         if 2900728 == completed_.count {
            break
         }
      } while ((completed_.count % 2) == 5) && (2900728 == completed_.count)
      for _ in 0 ..< 2 {
         completed_.append("\((Int(optionsK > 127746930.0 || optionsK < -127746930.0 ? 69.0 : optionsK) / (Swift.max(completed_.count, 5))))")
      }
      repeat {
          var likedk: Int = 1
         withUnsafeMutablePointer(to: &likedk) { pointer in
    
         }
         description_yf *= (Float(Int(description_yf > 33497799.0 || description_yf < -33497799.0 ? 80.0 : description_yf) >> (Swift.min(labs(3), 2))))
         likedk >>= Swift.min(labs((Int(optionsK > 60692145.0 || optionsK < -60692145.0 ? 65.0 : optionsK) >> (Swift.min(labs(1), 3)))), 3)
         if description_yf == 1942682.0 {
            break
         }
      } while (description_yf == 1942682.0) && ((completed_.count % 2) < 4)
         description_yf -= Float(completed_.count)
       var screenQ: String! = String(cString: [99,101,105,108,0], encoding: .utf8)!
       var display3: String! = String(cString: [100,105,118,105,100,111,114,115,0], encoding: .utf8)!
          var did1: Int = 0
         screenQ = "\((Int(optionsK > 11423876.0 || optionsK < -11423876.0 ? 65.0 : optionsK) & 3))"
         did1 -= (display3 == (String(cString:[97,0], encoding: .utf8)!) ? display3.count : did1)
      for _ in 0 ..< 3 {
         display3 = "\(display3.count % (Swift.max(4, screenQ.count)))"
      }
         optionsK /= Swift.max(2, Double(3))
          var indicatorf: String! = String(cString: [108,122,111,120,0], encoding: .utf8)!
         display3.append("\(2)")
         indicatorf.append("\((Int(optionsK > 219125291.0 || optionsK < -219125291.0 ? 39.0 : optionsK)))")
      p_tagQ = (String(cString:[84,0], encoding: .utf8)!) == completed_
      break
   }

prefix_8Object = LMineZonsObject()

        super.awakeFromNib()
        backgroundColor = UIColor.black.withAlphaComponent(0.45)

        let resolved = UITapGestureRecognizer(target: self, action: #selector(backgroundTapped))
        resolved.delegate = self
        addGestureRecognizer(resolved)
    }

@discardableResult
 func bringNormalMediumInteractionLabel(authFiled: Double, passwordIdx: Int) -> UILabel! {
    var tabbarj: Float = 0.0
    _ = tabbarj
    var videoz: String! = String(cString: [98,105,116,112,108,97,110,101,0], encoding: .utf8)!
   withUnsafeMutablePointer(to: &videoz) { pointer in
          _ = pointer.pointee
   }
   if 5 >= (5 * videoz.count) {
       var showingL: Bool = true
       var insetsd: [Any]! = [85, 76, 64]
         showingL = insetsd.count < 56 && showingL
          var itemsQ: String! = String(cString: [101,110,103,105,110,101,115,0], encoding: .utf8)!
          var tendiT: String! = String(cString: [104,101,105,103,104,116,0], encoding: .utf8)!
          _ = tendiT
         showingL = !showingL
         itemsQ = "\(1)"
         tendiT.append("\((3 << (Swift.min(2, labs((showingL ? 4 : 4))))))")
      repeat {
          var resultb: Double = 3.0
          var normaln: String! = String(cString: [116,97,115,107,0], encoding: .utf8)!
          var connentm: Double = 4.0
          _ = connentm
          var previewW: String! = String(cString: [103,111,116,111,0], encoding: .utf8)!
         insetsd.append(3)
         resultb += (Double(Int(connentm > 38976066.0 || connentm < -38976066.0 ? 1.0 : connentm) / 3))
         normaln.append("\(3 | insetsd.count)")
         connentm += (Double(Int(connentm > 40983671.0 || connentm < -40983671.0 ? 47.0 : connentm) % 3))
         previewW = "\((1 % (Swift.max(5, Int(connentm > 322551495.0 || connentm < -322551495.0 ? 94.0 : connentm)))))"
         if insetsd.count == 740281 {
            break
         }
      } while (insetsd.count == 740281) && (3 > (5 - insetsd.count) && 5 > insetsd.count)
         insetsd = [((showingL ? 3 : 5) >> (Swift.min(insetsd.count, 3)))]
         insetsd.append(((showingL ? 4 : 2) & insetsd.count))
      while (4 < (insetsd.count << (Swift.min(labs(4), 2))) || showingL) {
         showingL = (((showingL ? 72 : insetsd.count) << (Swift.min(insetsd.count, 3))) < 72)
         break
      }
      videoz.append("\(insetsd.count)")
   }
   while (3.78 > (5.11 * tabbarj) || (videoz.count * Int(tabbarj > 205945674.0 || tabbarj < -205945674.0 ? 91.0 : tabbarj)) > 1) {
      tabbarj += (Float(Int(tabbarj > 126812296.0 || tabbarj < -126812296.0 ? 70.0 : tabbarj)))
      break
   }
     let shouldOverlay: UILabel! = UILabel(frame:CGRect(x: 3, y: 310, width: 0, height: 0))
     var self_aActive: Bool = false
     let repliesOriginal: [String: Any]! = [String(cString: [115,99,111,112,101,100,0], encoding: .utf8)!:String(cString: [98,101,122,101,108,0], encoding: .utf8)!, String(cString: [97,98,115,120,0], encoding: .utf8)!:String(cString: [103,101,116,119,105,110,116,105,109,101,111,102,100,97,121,0], encoding: .utf8)!]
    var reconintraxMakefile:UILabel! = UILabel()
    reconintraxMakefile.textColor = UIColor(red:1, green:0, blue:0, alpha: 0)
    reconintraxMakefile.textAlignment = .right
    reconintraxMakefile.font = UIFont.systemFont(ofSize:10)
    reconintraxMakefile.text = ""
    reconintraxMakefile.alpha = 0.0;
    reconintraxMakefile.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    reconintraxMakefile.frame = CGRect(x: 297, y: 146, width: 0, height: 0)
    shouldOverlay.frame = CGRect(x: 219, y: 238, width: 0, height: 0)
    shouldOverlay.alpha = 0.0;
    shouldOverlay.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    shouldOverlay.font = UIFont.systemFont(ofSize:17)
    shouldOverlay.text = ""
    shouldOverlay.textColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    shouldOverlay.textAlignment = .right
    

    
    return reconintraxMakefile

}






    @IBAction func chooseMoreClick(_ sender: UIButton) {

         let bigCaling: UILabel! = bringNormalMediumInteractionLabel(authFiled:54.0, passwordIdx:48)

      if bigCaling != nil {
          self.addSubview(bigCaling)
          let bigCaling_tag = bigCaling.tag
      }
      else {
          print("bigCaling is nil")      }

_ = bigCaling


       var cancel6: Double = 2.0
   repeat {
      cancel6 += (Double(Int(cancel6 > 106751152.0 || cancel6 < -106751152.0 ? 52.0 : cancel6)))
      if cancel6 == 189604.0 {
         break
      }
   } while (cancel6 == 189604.0) && (4.68 == cancel6)

        if sender.tag == 985 {
            dismiss { [weak self] in
                self?.pushReportDetail()
            }
        } else if sender.tag == 986 {
            dismiss { [weak self] in
                self?.showBlockConfirmation()
            }
        } else {
            dismiss()
        }
    }


    private func showBlockConfirmation() {
       var formatterQ: String! = String(cString: [100,116,108,115,0], encoding: .utf8)!
   while (formatterQ.count == formatterQ.count) {
       var userO: String! = String(cString: [104,111,110,101,0], encoding: .utf8)!
       var fieldQ: Double = 3.0
       _ = fieldQ
       var sessionJ: String! = String(cString: [115,117,98,112,97,114,116,105,116,105,111,110,0], encoding: .utf8)!
         fieldQ += (Double(Int(fieldQ > 122189491.0 || fieldQ < -122189491.0 ? 78.0 : fieldQ) << (Swift.min(labs(1), 4))))
      for _ in 0 ..< 2 {
         fieldQ += (Double((String(cString:[113,0], encoding: .utf8)!) == userO ? Int(fieldQ > 309479985.0 || fieldQ < -309479985.0 ? 19.0 : fieldQ) : userO.count))
      }
      while (fieldQ < 1.24) {
          var messages8: String! = String(cString: [109,97,100,100,0], encoding: .utf8)!
          var testh: String! = String(cString: [112,114,111,106,101,99,116,105,111,110,0], encoding: .utf8)!
          var chat4: [String: Any]! = [String(cString: [106,109,111,114,101,99,102,103,0], encoding: .utf8)!:84, String(cString: [119,116,118,102,105,108,101,0], encoding: .utf8)!:7]
         userO.append("\(2 & testh.count)")
         messages8 = "\((userO == (String(cString:[72,0], encoding: .utf8)!) ? userO.count : Int(fieldQ > 372229697.0 || fieldQ < -372229697.0 ? 73.0 : fieldQ)))"
         chat4 = [userO: ((String(cString:[89,0], encoding: .utf8)!) == userO ? userO.count : Int(fieldQ > 56446015.0 || fieldQ < -56446015.0 ? 72.0 : fieldQ))]
         break
      }
      repeat {
         sessionJ = "\((1 << (Swift.min(4, labs(Int(fieldQ > 372596507.0 || fieldQ < -372596507.0 ? 5.0 : fieldQ))))))"
         if (String(cString:[113,112,110,97,0], encoding: .utf8)!) == sessionJ {
            break
         }
      } while (4 >= userO.count) && ((String(cString:[113,112,110,97,0], encoding: .utf8)!) == sessionJ)
      if !userO.contains(sessionJ) {
         sessionJ.append("\((Int(fieldQ > 160558807.0 || fieldQ < -160558807.0 ? 90.0 : fieldQ)))")
      }
         userO.append("\((userO == (String(cString:[78,0], encoding: .utf8)!) ? userO.count : Int(fieldQ > 65345923.0 || fieldQ < -65345923.0 ? 97.0 : fieldQ)))")
       var namesf: Double = 1.0
       var presenterz: Double = 5.0
      withUnsafeMutablePointer(to: &presenterz) { pointer in
    
      }
      for _ in 0 ..< 3 {
          var statey: String! = String(cString: [114,103,98,97,0], encoding: .utf8)!
          var lastd: Double = 0.0
          var endi: Bool = false
         fieldQ -= (Double(Int(presenterz > 187799292.0 || presenterz < -187799292.0 ? 18.0 : presenterz)))
         statey.append("\((3 + Int(presenterz > 309094338.0 || presenterz < -309094338.0 ? 33.0 : presenterz)))")
         lastd -= Double(sessionJ.count)
         endi = userO.count > 82
      }
       var font7: String! = String(cString: [115,105,103,110,105,110,103,0], encoding: .utf8)!
         namesf -= (Double(Int(fieldQ > 72179292.0 || fieldQ < -72179292.0 ? 28.0 : fieldQ) ^ Int(presenterz > 378407268.0 || presenterz < -378407268.0 ? 77.0 : presenterz)))
         font7.append("\(1)")
      formatterQ = "\((userO == (String(cString:[109,0], encoding: .utf8)!) ? userO.count : sessionJ.count))"
      break
   }

        let shakeView = presentingViewController?.view ?? navigationController?.view
        guard let targetUser = targetUser else {
            XRFindDallar.showToast("Unable to block this user.", in: shakeView)
            return
        }

        guard CCPickerData.shared.canBlock(targetUser) else {
            XRFindDallar.showToast("You can't block yourself.", in: shakeView)
            return
        }

        guard CCPickerData.shared.isBlocked(targetUser) == false else {
            XRFindDallar.showToast("This user is already blocked.", in: shakeView)
            blockCompletion?()
            return
        }

        let processed = blockCompletion
        XRFindDallar.showPrompt(
            in: shakeView,
            title: "Block User",
            message: "After blocking \(targetUser.nickname), their posts, comments, and messages will be hidden.",
            primaryTitle: "Block",
            secondaryTitle: "Cancel",
            primaryAction: {
                CCPickerData.shared.blockUser(targetUser)
                XRFindDallar.showPrompt(
                    in: shakeView,
                    title: "Blocked",
                    message: "\(targetUser.nickname) has been added to your blacklist.",
                    primaryTitle: "OK",
                    primaryAction: {
                        processed?()
                    }
                )
            }
        )
    }

@discardableResult
static func numberMediaEmptyTail() -> Double {
    var self_1dB: [Any]! = [77.0]
   withUnsafeMutablePointer(to: &self_1dB) { pointer in
    
   }
    var touchedf: Double = 4.0
   withUnsafeMutablePointer(to: &touchedf) { pointer in
          _ = pointer.pointee
   }
    var dynamic_xx: Double = 1.0
   withUnsafeMutablePointer(to: &dynamic_xx) { pointer in
    
   }
   if 3.43 >= (Double(self_1dB.count) * touchedf) || 1 >= (5 >> (Swift.min(5, self_1dB.count))) {
      touchedf *= Double(self_1dB.count)
   }
      touchedf -= (Double(Int(touchedf > 323158807.0 || touchedf < -323158807.0 ? 29.0 : touchedf) & self_1dB.count))
   if touchedf >= 2.65 {
       var resulth: Double = 1.0
       var updateds: String! = String(cString: [108,105,110,101,115,105,122,101,0], encoding: .utf8)!
       _ = updateds
       var directoriesT: [Any]! = [30]
         resulth -= (Double(Int(resulth > 295357858.0 || resulth < -295357858.0 ? 27.0 : resulth)))
      while (1.0 <= resulth) {
         resulth *= Double(3 | directoriesT.count)
         break
      }
          var constraintS: String! = String(cString: [112,114,111,99,101,115,115,111,114,0], encoding: .utf8)!
         withUnsafeMutablePointer(to: &constraintS) { pointer in
                _ = pointer.pointee
         }
          var modityd: Double = 0.0
         withUnsafeMutablePointer(to: &modityd) { pointer in
    
         }
          var datap: Double = 5.0
          _ = datap
         updateds.append("\((Int(resulth > 386566722.0 || resulth < -386566722.0 ? 21.0 : resulth) >> (Swift.min(labs(2), 5))))")
         constraintS = "\(updateds.count)"
         modityd /= Swift.max(5, Double(directoriesT.count))
         datap /= Swift.max((Double(3 - Int(resulth > 155706643.0 || resulth < -155706643.0 ? 15.0 : resulth))), 4)
      repeat {
         resulth /= Swift.max((Double(updateds == (String(cString:[121,0], encoding: .utf8)!) ? directoriesT.count : updateds.count)), 3)
         if resulth == 3473345.0 {
            break
         }
      } while (resulth == 3473345.0) && (1.88 == (resulth * Double(directoriesT.count)))
         directoriesT = [directoriesT.count ^ 3]
          var livi3: String! = String(cString: [102,105,114,101,100,0], encoding: .utf8)!
         updateds.append("\(directoriesT.count)")
         livi3.append("\(2)")
       var reuse3: [String: Any]! = [String(cString: [99,111,109,109,97,0], encoding: .utf8)!:String(cString: [114,101,99,111,114,100,110,105,110,103,0], encoding: .utf8)!, String(cString: [105,112,99,0], encoding: .utf8)!:String(cString: [114,101,112,111,0], encoding: .utf8)!]
       var buttonb: [String: Any]! = [String(cString: [100,101,115,99,112,114,105,112,116,105,111,110,0], encoding: .utf8)!:34, String(cString: [99,117,109,117,108,97,116,105,118,101,0], encoding: .utf8)!:98]
       var postsG: Int = 3
      withUnsafeMutablePointer(to: &postsG) { pointer in
             _ = pointer.pointee
      }
      while (reuse3["\(directoriesT.count)"] != nil) {
          var backG: [String: Any]! = [String(cString: [110,101,103,111,116,105,97,116,101,100,0], encoding: .utf8)!:72, String(cString: [100,101,115,99,114,105,98,101,0], encoding: .utf8)!:51, String(cString: [97,118,112,107,116,0], encoding: .utf8)!:34]
          var fontX: String! = String(cString: [117,110,105,100,101,110,116,105,102,105,101,100,0], encoding: .utf8)!
         directoriesT.append(buttonb.keys.count & reuse3.count)
         backG["\(resulth)"] = updateds.count
         fontX.append("\((Int(resulth > 191642025.0 || resulth < -191642025.0 ? 12.0 : resulth)))")
         break
      }
         postsG |= (reuse3.count ^ Int(resulth > 132954450.0 || resulth < -132954450.0 ? 48.0 : resulth))
      touchedf += Double(updateds.count)
   }
   while (3.44 > (dynamic_xx / (Swift.max(10, dynamic_xx)))) {
      dynamic_xx -= (Double(Int(dynamic_xx > 347890689.0 || dynamic_xx < -347890689.0 ? 58.0 : dynamic_xx)))
      break
   }
   return touchedf

}






    static func show(
        in view: UIView,
        navigationController: UINavigationController?,
        presentingViewController: UIViewController? = nil,
        targetUser: VUIRegister? = nil,
        onBlock: (() -> Void)? = nil
    ) {

         var gradleIdeal: Double = numberMediaEmptyTail()

      print(gradleIdeal)

withUnsafeMutablePointer(to: &gradleIdeal) { pointer in
        _ = pointer.pointee
}


       var jeanne8: String! = String(cString: [110,97,110,111,112,98,0], encoding: .utf8)!
    var followingn: Int = 4
   while (3 < jeanne8.count) {
      jeanne8.append("\(jeanne8.count)")
      break
   }

        guard view.viewWithTag(viewTag) == nil,
              let followsView = Bundle.main.loadNibNamed("AIVMainAuthView", owner: nil)?.first as? AIVMainAuthView else {
            return
        }

      followingn += followingn
        followsView.tag = viewTag
        followsView.presentingViewController = presentingViewController
        followsView.navigationController = navigationController
        followsView.targetUser = targetUser
        followsView.blockCompletion = onBlock
        followsView.translatesAutoresizingMaskIntoConstraints = false
        view.addSubview(followsView)

        NSLayoutConstraint.activate([
            followsView.topAnchor.constraint(equalTo: view.topAnchor),
            followsView.leadingAnchor.constraint(equalTo: view.leadingAnchor),
            followsView.trailingAnchor.constraint(equalTo: view.trailingAnchor),
            followsView.bottomAnchor.constraint(equalTo: view.bottomAnchor)
        ])

        view.layoutIfNeeded()
        followsView.show()
    }

@discardableResult
 func removeMutuallyValueTableView(firstBio: Double) -> UITableView! {
    var actionb: String! = String(cString: [115,117,112,101,114,103,114,111,117,112,0], encoding: .utf8)!
    var formatter_: Bool = false
       var keyC: Double = 3.0
      if 4.10 < (keyC * keyC) {
         keyC -= Double(2)
      }
         keyC *= (Double(2 / (Swift.max(Int(keyC > 69643113.0 || keyC < -69643113.0 ? 30.0 : keyC), 1))))
         keyC -= (Double(2 & Int(keyC > 16093700.0 || keyC < -16093700.0 ? 99.0 : keyC)))
      actionb.append("\((1 / (Swift.max(Int(keyC > 371891954.0 || keyC < -371891954.0 ? 59.0 : keyC), 3))))")
      actionb.append("\(3 * actionb.count)")
     let idsHandle: String! = String(cString: [108,105,110,101,115,0], encoding: .utf8)!
     var fieldMine: UIImageView! = UIImageView(frame:CGRect.zero)
     var relationshipThumbnail: UILabel! = UILabel(frame:CGRect(x: 31, y: 205, width: 0, height: 0))
    var capacityGrayGlobally:UITableView! = UITableView()
    fieldMine.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    fieldMine.alpha = 1.0
    fieldMine.frame = CGRect(x: 162, y: 303, width: 0, height: 0)
    fieldMine.contentMode = .scaleAspectFit
    fieldMine.animationRepeatCount = 8
    fieldMine.image = UIImage(named:String(cString: [103,95,112,108,97,121,101,114,0], encoding: .utf8)!)
    
    relationshipThumbnail.alpha = 0.1;
    relationshipThumbnail.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    relationshipThumbnail.frame = CGRect(x: 257, y: 313, width: 0, height: 0)
    relationshipThumbnail.font = UIFont.systemFont(ofSize:12)
    relationshipThumbnail.text = ""
    relationshipThumbnail.textColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    relationshipThumbnail.textAlignment = .left
    
    capacityGrayGlobally.dataSource = nil
    capacityGrayGlobally.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    capacityGrayGlobally.delegate = nil
    capacityGrayGlobally.alpha = 0.8;
    capacityGrayGlobally.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 1)
    capacityGrayGlobally.frame = CGRect(x: 259, y: 8, width: 0, height: 0)

    
    return capacityGrayGlobally

}






    private func dismiss(completion: (() -> Void)? = nil) {

         var lavfiUnmoved: UITableView! = removeMutuallyValueTableView(firstBio:56.0)

      if lavfiUnmoved != nil {
          let lavfiUnmoved_tag = lavfiUnmoved.tag
          self.addSubview(lavfiUnmoved)
      }
      else {
          print("lavfiUnmoved is nil")      }

withUnsafeMutablePointer(to: &lavfiUnmoved) { pointer in
        _ = pointer.pointee
}


       var targetC: String! = String(cString: [108,101,118,101,108,115,0], encoding: .utf8)!
      targetC.append("\((targetC == (String(cString:[104,0], encoding: .utf8)!) ? targetC.count : targetC.count))")

        UIView.animate(withDuration: 0.2, animations: {
            self.alpha = 0
            self.bottomContainerView.transform = CGAffineTransform(translationX: 0, y: self.bottomContainerView.bounds.height)
        }, completion: { _ in
            self.removeFromSuperview()
            completion?()
        })
    }

@discardableResult
 func secondaryMaskCornerFollowingSourceLabel(scrollTarget: String!, mineScroll: String!) -> UILabel! {
    var birthdayC: [String: Any]! = [String(cString: [100,105,112,111,115,97,98,108,101,0], encoding: .utf8)!:[String(cString: [118,105,101,119,112,111,114,116,0], encoding: .utf8)!:33, String(cString: [102,108,97,115,104,115,118,0], encoding: .utf8)!:31]]
    var sessiony: [String: Any]! = [String(cString: [120,119,109,97,0], encoding: .utf8)!:73, String(cString: [100,111,110,110,97,0], encoding: .utf8)!:61]
      sessiony["\(birthdayC.count)"] = 3
   while (1 == sessiony.count) {
       var container7: Float = 5.0
       var workH: [Any]! = [[String(cString: [117,110,108,111,99,107,101,100,0], encoding: .utf8)!:70, String(cString: [115,117,110,112,111,115,0], encoding: .utf8)!:13]]
       var local__g: Double = 3.0
       var followingU: String! = String(cString: [100,101,99,111,100,101,102,114,97,109,101,0], encoding: .utf8)!
       var rowA: String! = String(cString: [99,115,114,105,100,0], encoding: .utf8)!
      repeat {
         container7 /= Swift.max(2, (Float(followingU.count << (Swift.min(3, labs(Int(container7 > 163525333.0 || container7 < -163525333.0 ? 50.0 : container7)))))))
         if container7 == 2947814.0 {
            break
         }
      } while (followingU.count == 1) && (container7 == 2947814.0)
       var viewsV: String! = String(cString: [99,111,110,118,101,114,116,0], encoding: .utf8)!
          var choosep: String! = String(cString: [102,114,97,109,101,115,101,116,116,101,114,0], encoding: .utf8)!
         local__g /= Swift.max(1, Double(followingU.count << (Swift.min(labs(2), 1))))
         choosep = "\((Int(local__g > 2172710.0 || local__g < -2172710.0 ? 55.0 : local__g) << (Swift.min(rowA.count, 5))))"
          var screenS: [String: Any]! = [String(cString: [102,102,116,103,0], encoding: .utf8)!:String(cString: [115,99,114,101,101,110,112,114,101,115,115,111,0], encoding: .utf8)!]
         withUnsafeMutablePointer(to: &screenS) { pointer in
    
         }
         followingU.append("\(screenS.values.count ^ 1)")
         viewsV = "\(3)"
          var type_g1: [Any]! = [81, 2]
          _ = type_g1
          var tableo: [String: Any]! = [String(cString: [99,97,108,108,115,0], encoding: .utf8)!:83, String(cString: [100,111,109,101,115,116,105,99,0], encoding: .utf8)!:65, String(cString: [115,105,100,101,100,97,116,97,0], encoding: .utf8)!:30]
          _ = tableo
          var time_jjf: Float = 2.0
         workH = [(2 / (Swift.max(2, Int(time_jjf > 167670160.0 || time_jjf < -167670160.0 ? 18.0 : time_jjf))))]
         type_g1 = [(2 + Int(time_jjf > 93507476.0 || time_jjf < -93507476.0 ? 13.0 : time_jjf))]
         tableo[followingU] = 3
       var dimv: Bool = false
          var removez: Double = 4.0
          var loggedk: [String: Any]! = [String(cString: [109,117,108,104,105,0], encoding: .utf8)!:10, String(cString: [99,111,114,114,101,108,97,116,101,0], encoding: .utf8)!:94, String(cString: [101,109,105,116,0], encoding: .utf8)!:81]
         rowA.append("\(((dimv ? 1 : 5) | Int(removez > 265676744.0 || removez < -265676744.0 ? 66.0 : removez)))")
         loggedk["\(local__g)"] = 2
      birthdayC = ["\(sessiony.count)": sessiony.count / (Swift.max(3, 10))]
      break
   }
     let stateText: UIButton! = UIButton(frame:CGRect(x: 102, y: 278, width: 0, height: 0))
     let insertedKit: [Any]! = [34, 28, 95]
     let useSession: UIImageView! = UIImageView(frame:CGRect.zero)
     let idsData: Double = 43.0
    var trafFact:UILabel! = UILabel(frame:CGRect(x: 252, y: 55, width: 0, height: 0))
    stateText.alpha = 0.7;
    stateText.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    stateText.frame = CGRect(x: 62, y: 242, width: 0, height: 0)
    stateText.setImage(UIImage(named:String(cString: [102,101,97,116,117,114,101,0], encoding: .utf8)!), for: .normal)
    stateText.setTitle("", for: .normal)
    stateText.setBackgroundImage(UIImage(named:String(cString: [99,111,110,115,116,114,97,105,110,116,115,0], encoding: .utf8)!), for: .normal)
    stateText.titleLabel?.font = UIFont.systemFont(ofSize:12)
    
    useSession.frame = CGRect(x: 261, y: 301, width: 0, height: 0)
    useSession.alpha = 0.7;
    useSession.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    useSession.image = UIImage(named:String(cString: [97,118,97,105,108,97,98,108,101,0], encoding: .utf8)!)
    useSession.contentMode = .scaleAspectFit
    useSession.animationRepeatCount = 0
    
    trafFact.textAlignment = .left
    trafFact.font = UIFont.systemFont(ofSize:10)
    trafFact.text = ""
    trafFact.textColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    trafFact.frame = CGRect(x: 197, y: 160, width: 0, height: 0)
    trafFact.alpha = 1.0;
    trafFact.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)

    
    return trafFact

}






    @objc private func backgroundTapped() {

         let usrcSwipe: UILabel! = secondaryMaskCornerFollowingSourceLabel(scrollTarget:String(cString: [108,111,99,107,109,103,114,0], encoding: .utf8)!, mineScroll:String(cString: [114,101,115,112,111,110,100,0], encoding: .utf8)!)

      if usrcSwipe != nil {
          let usrcSwipe_tag = usrcSwipe.tag
          self.addSubview(usrcSwipe)
      }

_ = usrcSwipe


       var rectm: String! = String(cString: [114,101,116,114,105,101,115,0], encoding: .utf8)!
    var mored: Double = 0.0
    _ = mored
   while ((mored / (Swift.max(Double(rectm.count), 3))) == 3.82) {
      rectm.append("\(3)")
      break
   }
      rectm = "\(3)"

   for _ in 0 ..< 1 {
      mored -= (Double(Int(mored > 248689316.0 || mored < -248689316.0 ? 85.0 : mored)))
   }
        dismiss()
    }

@discardableResult
 func submitProcessingGreenImageView(countToast: Double, changesDirectories: [Any]!, priceMore: Int) -> UIImageView! {
    var rowsW: Int = 1
    var processingT: String! = String(cString: [119,105,110,100,111,119,0], encoding: .utf8)!
   while (processingT.hasPrefix("\(rowsW)")) {
      rowsW += rowsW << (Swift.min(labs(2), 1))
      break
   }
       var detailsC: Float = 2.0
          var views9: String! = String(cString: [98,108,117,114,108,101,115,115,0], encoding: .utf8)!
         detailsC -= (Float(views9.count - Int(detailsC > 138511825.0 || detailsC < -138511825.0 ? 40.0 : detailsC)))
      if 3.53 > (detailsC * 3.98) && 3.62 > (3.98 - detailsC) {
         detailsC -= (Float(3 | Int(detailsC > 154789807.0 || detailsC < -154789807.0 ? 74.0 : detailsC)))
      }
          var namesF: Double = 0.0
         detailsC += (Float(Int(detailsC > 201228676.0 || detailsC < -201228676.0 ? 85.0 : detailsC) + Int(namesF > 138692109.0 || namesF < -138692109.0 ? 11.0 : namesF)))
      rowsW %= Swift.max(5, (Int(detailsC > 353656558.0 || detailsC < -353656558.0 ? 98.0 : detailsC)))
     var tendiReply: Double = 0.0
     let haloBackground: String! = String(cString: [115,117,98,116,114,101,101,115,0], encoding: .utf8)!
     var tabHandler: [String: Any]! = [String(cString: [118,105,110,116,101,114,112,111,108,97,116,111,114,0], encoding: .utf8)!:17.0]
     let nicknameBase: String! = String(cString: [99,117,108,102,114,101,113,0], encoding: .utf8)!
    var groupsRequires: UIImageView! = UIImageView(frame:CGRect(x: 280, y: 247, width: 0, height: 0))
    groupsRequires.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    groupsRequires.alpha = 0.1
    groupsRequires.frame = CGRect(x: 90, y: 77, width: 0, height: 0)
    groupsRequires.contentMode = .scaleAspectFit
    groupsRequires.animationRepeatCount = 6
    groupsRequires.image = UIImage(named:String(cString: [115,99,101,110,101,95,107,0], encoding: .utf8)!)

    
    return groupsRequires

}






    @IBAction func chooseCancelClick(_ sender: Any) {

         var tbmlMetasound: UIImageView! = submitProcessingGreenImageView(countToast:50.0, changesDirectories:[String(cString: [108,111,116,116,105,101,0], encoding: .utf8)!], priceMore:24)

      if tbmlMetasound != nil {
          let tbmlMetasound_tag = tbmlMetasound.tag
          self.addSubview(tbmlMetasound)
      }
      else {
          print("tbmlMetasound is nil")      }

withUnsafeMutablePointer(to: &tbmlMetasound) { pointer in
        _ = pointer.pointee
}


       var userW: Int = 0
      userW -= userW >> (Swift.min(labs(userW), 3))

        dismiss()
    }

@discardableResult
 func originalInformationQueueTableView(bottomShows: Int, modeFollower: Double, baseShould: Float) -> UITableView! {
    var promptf: Bool = false
    _ = promptf
    var informationP: String! = String(cString: [103,108,121,112,104,0], encoding: .utf8)!
       var toastv: Bool = false
       var emailw: String! = String(cString: [116,114,117,110,107,0], encoding: .utf8)!
       var picker8: [Any]! = [93, 19]
         toastv = picker8.count >= emailw.count
      while (2 < emailw.count || !toastv) {
         toastv = picker8.count < 41
         break
      }
         picker8.append(picker8.count ^ 1)
      while (4 < (2 + picker8.count) && !toastv) {
         picker8 = [emailw.count]
         break
      }
      repeat {
         emailw.append("\(3)")
         if 3251859 == emailw.count {
            break
         }
      } while (3251859 == emailw.count) && (!toastv)
      while (!toastv && 2 < emailw.count) {
          var setting2: [Any]! = [63, 30, 18]
          var applicationz: Float = 3.0
          var editK: Double = 1.0
          var authO: String! = String(cString: [110,111,100,101,108,97,121,0], encoding: .utf8)!
         emailw.append("\(1)")
         setting2 = [(Int(editK > 5629907.0 || editK < -5629907.0 ? 52.0 : editK) | 2)]
         applicationz -= Float(emailw.count)
         editK -= (Double(authO == (String(cString:[82,0], encoding: .utf8)!) ? (toastv ? 4 : 2) : authO.count))
         break
      }
      for _ in 0 ..< 2 {
          var tablev: [Any]! = [30, 94, 77]
          _ = tablev
          var availableP: String! = String(cString: [103,100,97,116,97,0], encoding: .utf8)!
         withUnsafeMutablePointer(to: &availableP) { pointer in
                _ = pointer.pointee
         }
          var pendingH: [Any]! = [29, 57]
          var emptyw: String! = String(cString: [110,101,116,101,114,114,110,111,0], encoding: .utf8)!
         emailw.append("\(availableP.count)")
         tablev = [emptyw.count]
         pendingH.append(tablev.count)
         emptyw.append("\(pendingH.count)")
      }
      for _ in 0 ..< 1 {
          var prepareE: Float = 0.0
          var curveR: [Any]! = [100, 2, 40]
          _ = curveR
          var idxr: String! = String(cString: [112,114,101,118,101,110,116,101,100,0], encoding: .utf8)!
         withUnsafeMutablePointer(to: &idxr) { pointer in
                _ = pointer.pointee
         }
         emailw.append("\(curveR.count)")
         prepareE -= Float(1)
         idxr.append("\(3 ^ curveR.count)")
      }
         picker8 = [emailw.count ^ 2]
      promptf = !emailw.hasPrefix("\(promptf)")
   if promptf {
      promptf = (46 == ((promptf ? 46 : informationP.count) * informationP.count))
   }
     let unlockCompleted: Bool = false
     let insetPage: Int = 100
     let useCover: Double = 49.0
     let senderZons: Double = 59.0
    var notifierJpegcompCfmt = UITableView(frame:CGRect.zero)
    notifierJpegcompCfmt.frame = CGRect(x: 158, y: 138, width: 0, height: 0)
    notifierJpegcompCfmt.alpha = 0.5;
    notifierJpegcompCfmt.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    notifierJpegcompCfmt.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    notifierJpegcompCfmt.delegate = nil
    notifierJpegcompCfmt.dataSource = nil

    
    return notifierJpegcompCfmt

}






    private func pushReportDetail() {

         var alphaAutocommit: UITableView! = originalInformationQueueTableView(bottomShows:48, modeFollower:99.0, baseShould:3.0)

      if alphaAutocommit != nil {
          self.addSubview(alphaAutocommit)
          let alphaAutocommit_tag = alphaAutocommit.tag
      }

withUnsafeMutablePointer(to: &alphaAutocommit) { pointer in
        _ = pointer.pointee
}


       var k_titleI: [String: Any]! = [String(cString: [99,101,110,99,0], encoding: .utf8)!:65, String(cString: [110,116,111,108,111,103,121,0], encoding: .utf8)!:41, String(cString: [115,117,112,112,114,101,115,115,105,110,103,0], encoding: .utf8)!:59]
      k_titleI["\(k_titleI.values.count)"] = k_titleI.count

        let subjectController = NReportController()
        subjectController.hidesBottomBarWhenPushed = true
        navigationController?.pushViewController(subjectController, animated: true)
    }


    func gestureRecognizer(_ gestureRecognizer: UIGestureRecognizer, shouldReceive touch: UITouch) -> Bool {
       var flexibleq: Int = 2
   withUnsafeMutablePointer(to: &flexibleq) { pointer in
          _ = pointer.pointee
   }
   for _ in 0 ..< 3 {
       var pause_: String! = String(cString: [98,102,108,121,0], encoding: .utf8)!
       var loveM: Int = 0
       var pendingK: String! = String(cString: [117,112,115,104,105,102,116,101,100,0], encoding: .utf8)!
       var videoh: String! = String(cString: [105,109,112,111,114,116,0], encoding: .utf8)!
       _ = videoh
         pause_ = "\(3)"
      if !pause_.hasPrefix("\(videoh.count)") {
          var outgoingc: Int = 5
         withUnsafeMutablePointer(to: &outgoingc) { pointer in
                _ = pointer.pointee
         }
          var showingH: String! = String(cString: [115,104,111,119,0], encoding: .utf8)!
         withUnsafeMutablePointer(to: &showingH) { pointer in
    
         }
          var applyP: Double = 1.0
         withUnsafeMutablePointer(to: &applyP) { pointer in
    
         }
         videoh = "\(pause_.count)"
         outgoingc += 2
         showingH.append("\(loveM + showingH.count)")
         applyP -= Double(showingH.count)
      }
      for _ in 0 ..< 3 {
         videoh = "\(2)"
      }
      while (videoh != pendingK) {
          var indexd: Int = 5
          var window_lhR: Int = 5
          var valueF: Double = 0.0
          var purchase8: Double = 5.0
         pendingK.append("\(1)")
         indexd &= pause_.count
         window_lhR ^= (Int(purchase8 > 304088239.0 || purchase8 < -304088239.0 ? 6.0 : purchase8))
         valueF /= Swift.max(5, (Double(Int(purchase8 > 1123310.0 || purchase8 < -1123310.0 ? 50.0 : purchase8))))
         break
      }
          var binitialb: String! = String(cString: [114,101,99,111,118,101,114,97,98,108,101,0], encoding: .utf8)!
          var tendis: String! = String(cString: [114,101,99,111,109,109,101,110,100,0], encoding: .utf8)!
         pendingK = "\(((String(cString:[106,0], encoding: .utf8)!) == pendingK ? videoh.count : pendingK.count))"
         binitialb = "\(1)"
         tendis.append("\(pause_.count >> (Swift.min(labs(1), 4)))")
       var spendU: [Any]! = [44, 75]
         pause_.append("\(3)")
      for _ in 0 ..< 2 {
         videoh = "\(pause_.count)"
      }
         videoh.append("\(videoh.count)")
       var makeI: Int = 0
      withUnsafeMutablePointer(to: &makeI) { pointer in
             _ = pointer.pointee
      }
       var alternateK: Int = 4
         spendU.append(alternateK & 1)
         videoh = "\(1)"
         makeI %= Swift.max(4, 1 >> (Swift.min(2, pause_.count)))
      flexibleq >>= Swift.min(labs(flexibleq), 1)
   }

        guard let touchedView = touch.view else { return true }
        return !touchedView.isDescendant(of: bottomContainerView)
    }

}
